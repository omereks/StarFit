<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.starfittwo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StarFitTwo">
        <activity android:name=".FragUserDetails"></activity>
        <activity
            android:name=".MainPageTab"
            android:label="@string/title_activity_main_page_tab"
            android:theme="@style/Theme.StarFitTwo.NoActionBar" />
        <activity android:name=".Loggin.RegisterUser" />
        <activity android:name=".Loggin.LogginActivity" />
        <activity android:name=".FoodFragment.FoodFrag" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>